name: test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - uses: xpol/setup-lua@v2
      - name: run 
        run: |
          cd ${{ github.workspace }}/src; pwd; ls ..; \
          lua l5.lua -t all 
      - run: echo "🍏 This job's status is ${{ job.status }}."
